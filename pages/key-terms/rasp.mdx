import { KeyTermLink } from '../../components/KeyTermLink.tsx'

# Runtime Application System Protection

A specific kind of Web Application Firewall (WAF) that often does HTTP Request and Response processing + deploying a Machine Learning (ML) algorithm inline or in a processing layer.

Yeah, I know. But it's very difficult to describe in plain English. I think that's actually about as well as I can do.
